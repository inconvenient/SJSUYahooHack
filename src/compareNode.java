import java.util.Comparator;

public class compareNode implements Comparator<error> {
		@Override
		public int compare(error o1, error o2) {
			// TODO Auto-generated method stub
			return o1.getNode().compareTo(o2.getNode());	
		}		
	}